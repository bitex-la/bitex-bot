module BitexBot
  # A CloseBuy represents an Ask on the remote exchange intended
  # to close one or several OpenBuy positions.
  # TODO: document attributes.
  class CloseBuy < ActiveRecord::Base; end
end
